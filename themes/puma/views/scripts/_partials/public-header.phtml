<?php
// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 WebTigers. Tiger™ and WebTigers™ are trademarks of WebTigers.
/**
 * PUMA public header — the marketing/site navbar (MIT, Tiger-original).
 *
 * Layout: [ brand ] [ main menu ] [ · search region · ] [ utility switchers ] [ sign up ] [ sign in ]
 *
 * Two header REGIONS let a feature contribute chrome without this theme file knowing about
 * it (the two-step view renders the action script first, so a view script can fill a region
 * that the layout — and this header — read afterward):
 *   - `tigerHeaderSearch` — the centered slot; a feature drops a search trigger here
 *     (TigerDocs fills it with its ⌘K launcher). Empty on pages that don't use it.
 *   - `tigerHeaderAuth`   — the sign-up/sign-in/dashboard slot; the theme renders a sensible
 *     default (below), but a feature may override the whole region.
 *
 * Behavior (switchers, prefs) lives in tiger.prefs.js.
 */
$identity   = Zend_Auth::getInstance()->hasIdentity() ? Zend_Auth::getInstance()->getIdentity() : null;
$authRegion = $this->placeholder('tigerHeaderAuth');
?>
<style>
/* Solutions mega-menu: a wide two-up card panel on desktop; stacks inside the collapsed nav on mobile. */
.tiger-mega .mega-card { transition: background-color .12s ease, transform .12s ease; }
.tiger-mega .mega-card:hover { background: var(--bs-tertiary-bg); }
.tiger-mega .mega-card-ghost { border: 1px dashed var(--bs-border-color); }
.tiger-mega .mega-card-ghost:hover { background: var(--bs-tertiary-bg); }
@media (min-width: 992px) { .tiger-mega .dropdown-menu { width: 620px; } }
</style>
<nav class="navbar navbar-expand-lg border-bottom sticky-top" style="background: var(--bs-body-bg);">
    <div class="container">
        <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/">
            <i class="fa-solid fa-paw text-primary"></i> <?= $this->escape($this->siteName ?? 'Tiger') ?>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#tigerNav"
                aria-controls="tigerNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="tigerNav">
            <ul class="navbar-nav mb-2 mb-lg-0">
                <!-- Solutions: a static mega-menu — HTML injected into the first dropdown. -->
                <li class="nav-item dropdown tiger-mega">
                    <a class="nav-link dropdown-toggle" href="#" id="tigerSolutions" role="button" data-bs-toggle="dropdown" aria-expanded="false">Solutions</a>
                    <div class="dropdown-menu border-0 shadow-lg p-3" aria-labelledby="tigerSolutions">
                        <div class="row g-2">
                            <div class="col-12 col-lg-6">
                                <a class="mega-card d-block h-100 text-decoration-none text-body rounded-3 p-3" href="/cms">
                                    <div class="d-flex gap-3">
                                        <i class="fa-solid fa-wand-magic-sparkles text-primary fs-4 mt-1"></i>
                                        <div>
                                            <div class="fw-semibold">Build Killer Websites</div>
                                            <div class="small text-secondary">No WordPress&reg; required</div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                            <div class="col-12 col-lg-6">
                                <a class="mega-card d-block h-100 text-decoration-none text-body rounded-3 p-3" href="/agency">
                                    <div class="d-flex gap-3">
                                        <i class="fa-solid fa-people-group text-primary fs-4 mt-1"></i>
                                        <div>
                                            <div class="fw-semibold">Agency Friendly</div>
                                            <div class="small text-secondary">Host hundreds of sites</div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                            <div class="col-12 col-lg-6">
                                <a class="mega-card d-block h-100 text-decoration-none text-body rounded-3 p-3" href="/vibe">
                                    <div class="d-flex gap-3">
                                        <i class="fa-solid fa-rocket text-primary fs-4 mt-1"></i>
                                        <div>
                                            <div class="fw-semibold">Vibe SaaS Startups</div>
                                            <div class="small text-secondary">A secure MVP in minutes, not days</div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                            <div class="col-12 col-lg-6">
                                <a class="mega-card mega-card-ghost d-block h-100 text-decoration-none text-secondary rounded-3 p-3" href="#">
                                    <div class="d-flex gap-3">
                                        <i class="fa-solid fa-circle-plus fs-4 mt-1"></i>
                                        <div>
                                            <div class="fw-semibold">What else could go here&hellip;?</div>
                                            <div class="small">Your next audience</div>
                                        </div>
                                    </div>
                                </a>
                            </div>
                        </div>
                    </div>
                </li>
                <li class="nav-item"><a class="nav-link" href="/#features">Features</a></li>
                <li class="nav-item"><a class="nav-link" href="/docs">Docs</a></li>
                <li class="nav-item"><a class="nav-link" href="https://github.com/WebTigers/Tiger" target="_blank" rel="noopener">GitHub</a></li>
            </ul>

            <!-- Centered search region: filled by a feature (e.g. TigerDocs ⌘K); collapses to nothing when empty. -->
            <div class="tiger-header-search mx-lg-auto my-2 my-lg-0"><?= $this->placeholder('tigerHeaderSearch') ?></div>

            <div class="d-flex align-items-center gap-1">
                <?= $this->render('_partials/lang-switcher.phtml') ?>
                <?= $this->render('_partials/theme-switcher.phtml') ?>
<?php if (!empty($this->skins)): ?>
                <?= $this->render('_partials/skin-switcher.phtml') ?>
<?php endif; ?>
                <!-- Auth region: theme default (sign up / sign in, or dashboard) unless a feature fills it. -->
<?php if (count($authRegion)): ?>
                <?= $authRegion ?>
<?php elseif ($identity): ?>
                <a class="btn btn-primary ms-2" href="/admin"><i class="fa-solid fa-gauge-high me-1"></i>Dashboard</a>
<?php else: ?>
                <a class="btn btn-link ms-2 text-decoration-none" href="/signup">Sign up</a>
                <a class="btn btn-outline-secondary" href="/login">Sign in</a>
<?php endif; ?>
            </div>
        </div>
    </div>
</nav>
